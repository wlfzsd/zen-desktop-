# Zen 上游代理补丁包

给 [irbis-sh/zen-desktop](https://github.com/irbis-sh/zen-desktop)（Zen 广告拦截器）添加**上游代理（HTTP 代理链式转发）**功能的补丁方案。以补丁形式交付：官方发布新版本后，重跑一次构建脚本即可在新版本上重建补丁版，无需重做任何工作。

> 对应上游开放中的 feature request：issue [#609 "Add support for chaining proxies"](https://github.com/irbis-sh/zen-desktop/issues/609)。

## 功能说明

把上游代理地址写进 **Zen 安装目录的 `upstream-proxy.txt` 配置文件**后，Zen 的**全部对外流量**都会先经过你指定的 HTTP 上游代理：

```
应用 → Zen（广告过滤/HTTPS 解析） → 上游代理（ip:端口） → 目标网站
```

**任意方式启动 Zen 都会自动读取该配置**（开始菜单/开机自启/双击 exe 均生效）；环境变量 `ZEN_UPSTREAM_PROXY` 仍可用作临时覆盖（优先级更高，随进程消亡）。系统/用户环境变量零改动（2026-09-17 用户禁令；2026-09-18 起配置文件为唯一常规入口）。

覆盖的出站路径（源码证据）：

| 出站路径 | 补丁挂钩点 |
|---------|-----------|
| 明文 HTTP 转发 | `requestTransport.Proxy`（proxy.go:263 路径） |
| HTTPS MITM 后的转发 | `requestTransport.Proxy`（proxy.go:463 路径） |
| CONNECT 透传隧道 | `tunnelDialContext`（proxy.go:520 路径） |
| WebSocket（ws://） | `tunnelDial`（websocket.go:24） |
| WebSocket over TLS（wss://） | `chainTLSDial`（websocket.go:15） |
| 过滤列表下载 / 证书 OCSP 等标准库请求 | 环境变量镜像（upstreamchain.go 的 init） |

配置格式（配置文件或环境变量，二者同格式）：

- `192.168.1.100:7890`（裸 ip:端口，自动按 http 处理）
- `http://192.168.1.100:7890`
- `http://user:pass@192.168.1.100:7890`（带认证）

**没有配置文件且未设变量 = 与原版行为完全一致**（已实测对照验证：同样流量，假上游日志零新增）。

安全设计：回环地址（127.0.0.1、localhost、::1）与上游代理自身地址永远直连不进链，防止本地服务被指到远程代理、以及 CONNECT 到自身的递归。

## 文件清单

| 文件 | 用途 |
|------|------|
| `zen-upstream-chain.patch` | 补丁本体：新增 `internal/proxy/upstreamchain.go` + `upstreamchain_test.go`，修改 `proxy.go`（3 处共 5 行）与 `websocket.go`（2 处） |
| `构建上游代理版Zen.ps1` | 一键脚本：拉取指定版本源码 → 打补丁 → 构建（官方 prod-noupdate 目标，禁自动更新）→ 备份并安装；`-UpstreamProxy` 顺带写入配置文件（网络抖动时自动降级离线重建） |
| `启动Zen（上游代理）.cmd` | 可选的便捷启动入口（双击）：Zen 现在自行读配置，此启动器只是普通启动方式之一 |
| `设置上游代理.ps1` | 写/读/清 Zen 安装目录的 `upstream-proxy.txt`（`-Proxy ip:端口` / `-Clear` / 不带参数查看） |
| `使用说明.md` | 本文档 |

## 使用方法

### 首次构建安装

```powershell
cd D:\Users\Administrator\Desktop\问题文件夹\Zen上游代理插件
.\构建上游代理版Zen.ps1                      # 最新版本，构建+安装
.\构建上游代理版Zen.ps1 -Version v0.25.1     # 指定版本
.\构建上游代理版Zen.ps1 -UpstreamProxy 1.2.3.4:7890   # 构建后顺带写入上游代理配置
```

前置条件（脚本会尽量自动处理）：

- Go（未装时脚本尝试 winget 安装；模块代理自动设为 goproxy.cn）
- Node.js ≥ 24.14（Zen 前端构建需要）
- git、网络可达 github.com（源码与 tag）

### 日常使用

```powershell
# 1) 设置/修改上游代理（写入 Zen 安装目录 upstream-proxy.txt）
.\设置上游代理.ps1 -Proxy 192.168.1.100:7890

# 2) 重启 Zen（任意方式：开始菜单/托盘退出后再开/双击 启动Zen（上游代理）.cmd 皆可）
#    日志出现 "chaining outbound traffic through <地址> (source: ...upstream-proxy.txt)" 即链式生效
#
# 取消上游代理（Zen 恢复直连）：
.\设置上游代理.ps1 -Clear
# 查看当前配置：
.\设置上游代理.ps1
```

> 配置文件是唯一常规入口：**不管怎么启动 Zen 都生效**（2026-09-18 起无需专用启动器）。`ZEN_UPSTREAM_PROXY` 环境变量仅作临时覆盖（如调试时指向别的上游），不设置不影响任何功能。

### 官方出新版后（核心卖点）

```powershell
.\构建上游代理版Zen.ps1        # 自动拉最新 tag → 重打补丁 → 重建 → 安装
```

补丁触点只有 4 个（`NewProxy` 内一行调用、`tunnel()` 一行拨号、`websocket.go` 两个分支）加一个独立新文件，未来版本冲突概率很低。万一打不上（脚本会明确报冲突），按提示手工迁移这几处即可，工作量是分钟级。

构建用的是官方 `prod-noupdate` 构建目标（`tasks/build/Taskfile-windows.yml:16-18`，注入 `selfupdate.NoSelfUpdate=true`），**补丁版不会被 Zen 的自动更新覆盖**。想升级就是重跑一次脚本。

## 验证结论（本机实测，v0.25.1 + 补丁版）

- 全仓 Go 测试通过（含新增 10 个链式测试：明文转发、MITM 转发、CONNECT 隧道、回环直连守卫、隧道字节保真、配置解析、门控开关等）
- 端到端（真实 exe + 真实流量）：
  - 设置 `ZEN_UPSTREAM_PROXY=http://127.0.0.1:18080` 指向本地假上游代理后：明文 HTTP 200、HTTPS 200，假上游日志出现 `GET http://www.baidu.com/`（绝对形式转发）与 `CONNECT www.baidu.com:443`（隧道），系统真实流量（如微信 `store.weixin.qq.com:443`）同样穿过假上游
  - 对照组（清除变量，同样流量）：HTTP 200 / HTTPS 200 正常，假上游日志**零新增**（169→169）
- 真实上游部署验证（sing-box `127.0.0.1:20122`）：直连 google 必败（000/8s）→ 走补丁版 Zen 链路后 `https://www.google.com` 返回 `<title>Google</title>`、`https://www.youtube.com` 200（338KB 真实页面）；浏览器同源证书栈（Invoke-WebRequest 无跳过参数）完全信任 Zen MITM 证书——日常浏览器使用无需任何额外配置
- **多轮交错基准（2026-09-18 凌晨，三条路径同窗采样）**：链式 Zen 的延迟**不劣于** sing-box 直连（google 中位 0.31-0.46s vs 直连 0.38-0.71s；连接复用使链式更快）；`accounts.google.com/RotateCookiesPage` 存在**与 Zen 无关的间歇性故障**（同一时间窗内 sing-box 直连同样失败、稍后同样恢复——该端点对代理出口 IP 最敏感）
- **韧性修复（2026-09-18）**：CONNECT 握手尊重浏览器取消（修复 goroutine/连接泄漏）＋ websocket 拨号加超时 ＋ 链式模式传输层参数收紧（`ResponseHeaderTimeout` 3min→30s：上游抖动时请求快速失败重试而非挂起数分钟；`IdleConnTimeout` 90s→30s：上游重启后的尸体连接及时清理，连接数有界不累积）。实测 45s/90s 空闲后请求 0.2-0.7s 秒回

## 注意事项

- 只支持 HTTP 上游代理（CONNECT 隧道），不支持 SOCKS5（如需可在此基础上加 `golang.org/x/net/proxy` 的 socks 拨号器）
- 上游代理若要求认证，用 `http://user:pass@ip:port` 形式
- 杀软（如 360）可能拦截新编译的未签名 exe：把安装目录加入白名单即可（本机已处理）
- Zen 首次启动会弹「安全警告」要求安装其本地生成的根证书（Zen 本体功能，与补丁无关），点是即可
- 补丁基于 v0.25.1 制作并实测；已验证 `proxy.go`/`websocket.go` 在 master 与该 tag 间无差异
